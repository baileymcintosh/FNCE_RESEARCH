# Copy this file to .env and fill in real values.
# .env is gitignored and should NEVER be committed.

# Your WRDS username (password is stored in ~/.pgpass after first login)
WRDS_USERNAME=your_wrds_username

# Optional: override default paths (defaults are relative to repo root)
# DATA_DIR=./data
# RESULTS_DIR=./results
